using System.Collections;
using UnityEngine;
// 하위 폴더에서 새항목 추가로 스크립트 파일을 만들면 폴더에맞는 이름으로 네임스페이가 자동으로 생성됩니다.
// 유니티 에디터에서도 반영됩니다. 
// 유니티 내에서 폴더만들땐 네임스페이스 지정안함
// using 키워드를 사용하면 코드내에 네임스페이스 하위 내용을 포함시킬수 있다
namespace Assets.Sprites
{
    public class NewBehaviourScript1 : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}